import React from 'react';

import Flow from '../../assets/bannertime-flow.gif';

const SplashViz = () => (
  <section className="splash-viz">
    <div className="container splash-viz__flow">
      <img src={ Flow }/>
    </div>
  </section>
);

export default SplashViz;
